import React from 'react'
import PropTypes from 'prop-types'

const Songs2RecordComponent = () => (
    <div>Hello
        {/* <ul>
            {data.map(a =>
                <li key={a.userId}>
                    Song: {a.song.name}
                </li>
            )}
        </ul> */}
    </div>
)

// const albumShape = {
//     id: PropTypes.string,
//     image: PropTypes.string,
//     name: PropTypes.string,
//     type: PropTypes.string,
// }

// const artistShape = {
//     id: PropTypes.string,
//     name: PropTypes.string,
//     type: PropTypes.string,
// }

// const songShape = {
//     id: PropTypes.string,
//     name: PropTypes.string,
//     type: PropTypes.string,
// }

// MySporifySongs2RecordComponent.prototype = {
//     data: PropTypes.arrayOf(PropTypes.shape({
//         userId: PropTypes.bool,
//         album: PropTypes.shape(albumShape),
//         artists: PropTypes.arrayOf(PropTypes.shape(artistShape)),
//         song: PropTypes.shape(songShape),
//         isSelected: PropTypes.bool
//     }))

// }

export default Songs2RecordComponent